<div class="container">
  <div class="row"

  	<% if current_page?(login_path) || current_page?(signup_path) || current_page?(new_event_session_path) || current_page?(new_user_session_path) %>
      style="padding-top: 10%;"
  	<% end %>

    >

    <div class="col-md-12 col-no-padding-x">
      <%= yield %>
      <%= render 'layouts/footer' %>
    </div>
  </div>
</div>
