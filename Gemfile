source 'https://rubygems.org'

group :development, :test do
    gem 'byebug'
end

group :development do
    gem 'web-console', '~> 2.0'
    gem 'spring'

    gem 'capistrano',         require: false
    gem 'capistrano-rbenv',   require: false
    gem 'capistrano-rails',   require: false
    gem 'capistrano-bundler', require: false
    gem 'capistrano3-puma',   require: false
end

# Deployment
gem 'puma'

# Rails version
gem 'rails', '4.2.5.2'

# Database
gem 'mysql2', '>= 0.3.13', '< 0.5'

# Use Uglifier as compressor for JavaScript assets
gem 'uglifier', '>= 1.3.0'

# Use CoffeeScript for .coffee assets and views
gem 'coffee-rails', '~> 4.1.0'

# Use jquery as the JavaScript library
gem 'jquery-rails'

# Turbolinks makes following links in your web application faster. Read more: https://github.com/rails/turbolinks
gem 'turbolinks'

# Build JSON APIs with ease. Read more: https://github.com/rails/jbuilder
gem 'jbuilder', '~> 2.0'

# bundle exec rake doc:rails generates the API under doc/api.
gem 'sdoc', '~> 0.4.0', group: :doc

# Integrating Bootstrap with SASS
gem 'sass-rails', '~> 5.0'
gem 'bootstrap-sass', '~> 3.3.6'

# Authentication
gem 'devise'
gem 'omniauth-facebook'
gem 'omniauth-twitter'
gem 'omniauth-linkedin'

#include this to get the original size avatar
gem 'linkedin'

gem 'omniauth-github'
gem "omniauth-google-oauth2"

# Image uploading
#gem 'paperclip', '~> 4.3', '>= 4.3.6'

# Font awesome
gem "font-awesome-rails"

# Allow ajax file upload
gem 'remotipart', '~> 1.2'

# Debugging
gem "better_errors"

# Lightbox
gem 'fancybox-rails'
gem 'fancybox2-rails'

# Jquery UI
gem 'jquery-ui-rails'

# Real-Time features
gem 'pusher'

# Jquery-pjax (Ajax page rendering + Push State)
gem 'jquery-pjax-rails', '~> 1.8'

# Manage read/unread status of ActiveRecord objects
gem 'unread'
